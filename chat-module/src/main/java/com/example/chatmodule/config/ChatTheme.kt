package com.example.chatmodule.config

/**
 * Enum for chat theme mode selection
 */
enum class ChatTheme {
    LIGHT,
    DARK,
    AUTO
}

// For compatibility with previous code, provide an alias
// (If needed, you can use: import com.example.chatmodule.config.ChatTheme as ChatThemeConfig) 